# Overview: 多传感器融合自动紧急避障系统

---

## 1. 系统总体目标

本系统基于多传感器融合，实现移动机器人在复杂环境下的自动紧急避障能力。
系统核心流程如下：

感知 → 融合 → 状态估计 → 风险评估（TTC） → 分级避障决策

---

## 2. 系统整体架构

### 2.1 数据流

```
LiDAR (/scan 或 /points)
Camera (/image + depth)
mmWave Radar (/radar_objects)
        ↓
YOLO Detection (/detections)
        ↓
Fusion Node (/tracked_objects)
        ↓
Decision Node (/cmd_vel)
```

---

## 3. 传感器节点设计

---

### 3.1 视觉感知节点（Camera + YOLO）

#### 输入：

* /image（RGB图像）
* /depth（深度图）

#### 输出：

* /detections（Detection2DArray）

#### 功能：

1. 图像预处理（去畸变）
2. 目标检测（YOLO）
3. 深度信息获取
4. 输出目标二维检测框 + 类别 + 置信度 + 深度

#### 输出数据结构：

```
Detection:
- bbox (x, y, w, h)
- class
- confidence
- depth
```

---

### 3.2 激光雷达节点（LiDAR）

#### 输入：

* 激光扫描数据

#### 输出：

* /scan 或 /points

#### 功能：

1. 数据滤波（去噪）
2. 点云生成（可选）
3. 障碍物几何信息提取

---

### 3.3 毫米波雷达节点（Radar）

#### 输入：

* 雷达原始数据

#### 输出：

* /radar_objects

#### 功能：

1. 目标检测
2. 速度估计（关键）
3. 距离测量

#### 输出数据结构：

```
RadarObject:
- position (x, y)
- velocity (vx, vy)
- confidence
```

---

## 4. 融合节点设计（Fusion Node）

---

### 4.1 输入接口

* /detections（视觉）
* /points 或 /scan（激光雷达）
* /radar_objects（毫米波雷达）
* TF（坐标变换）

---

### 4.2 输出接口

```
/tracked_objects
```

```
TrackedObject:
- id
- class
- position (x, y)
- velocity (vx, vy)
- confidence
- source_flag（来源传感器）
```

---

### 4.3 融合流程

#### Step 1：时间同步

* 使用近似时间同步机制
* 保证多传感器数据对齐

---

#### Step 2：空间对齐

* 统一到 base_link 坐标系
* 使用 TF 进行坐标变换

---

#### Step 3：目标生成

1. YOLO检测框 → 3D点（结合深度）
2. LiDAR → 点云聚类
3. Radar → 目标点 + 速度

---

#### Step 4：数据关联

目标：不同传感器观测对应同一真实目标

方法：

* 欧氏距离匹配
* 投影匹配（3D→2D）

---

#### Step 5：自适应融合（核心创新）

##### （1）问题本质

不同传感器在不同环境下可靠性不同：

* 雨雾：视觉下降
* 远距离：LiDAR稀疏
* 动态目标：雷达优势明显

---

##### （2）基于贝叶斯思想的融合

目标状态记为：

```
X = 目标真实状态
```

不同传感器观测为：

```
Z_cam, Z_lidar, Z_radar
```

融合模型：

P(X | Z) ∝ P(Z | X) P(X)

其中：

* P(Z | X)：观测模型（传感器可信度）
* P(X)：先验（来自历史跟踪）

---

##### （3）自适应权重分配

定义权重：

```
w_i ∝ 传感器可信度
```

权重计算依据：

* 置信度（confidence）
* 环境条件（可扩展）
* 历史一致性

---

##### （4）假设检验机制（关键增强）

对每个观测进行一致性检验：

H0：该观测属于当前目标
H1：该观测为噪声/误检

判定方法：

* 距离阈值检验
* 马氏距离检验（推荐）

若不满足 H0 → 剔除该观测

---

#### Step 6：状态估计（跟踪）

采用状态向量：

```
x = [px, py, vx, vy]
```

方法：

* Kalman Filter（实时）
* 输出平滑轨迹与速度

---

### 4.4 融合节点输出

```
TrackedObjectArray:
- 多目标列表
- 稳定ID
- 平滑位置
- 速度信息
```

---

## 5. 基于TTC的避障决策节点设计

---

### 5.1 输入

* /tracked_objects

---

### 5.2 TTC计算

TTC = \frac{d}{v}

其中：

* d：目标距离
* v：相对速度

---

### 5.3 风险评估

对每个目标计算 TTC：

* TTC 越小 → 风险越高

---

### 5.4 分级避障策略

| TTC范围         | 行为   |
| ------------- | ---- |
| TTC > 5s      | 正常行驶 |
| 3s < TTC ≤ 5s | 预警   |
| 1s < TTC ≤ 3s | 减速   |
| TTC ≤ 1s      | 紧急停车 |
| 特殊情况          | 绕行   |

---

### 5.5 决策输出

```
/cmd_vel
```

包含：

* 线速度
* 角速度

---

## 6. 系统特点总结

---

### 6.1 优点

1. 多传感器互补，提高鲁棒性
2. 自适应融合，提高环境适应能力
3. 基于TTC，具备物理意义

---

### 6.2 可扩展性

* 可接入更多传感器
* 可替换融合算法
* 可扩展为自动驾驶系统

---

# 注意事项（非常重要）！！！！！
1. 这只是一个大纲，关于depthai节点和rplidar节点你不需要改动直接掉包就可以，如果需要新加入东西，那就新建节点然后掉包，
   核心是不要改动这两个文件夹下的所有内容！
2. 你需要详细设计融合节点和避障决策节点，有关算法需要在前面用伪代码的形式用中文说明方便我后期查看也方便你修改，其中的数学环节更要仔细说明，这样我们方便

3. 你有什么还需的条件随时问我我们一起商议。在遵循项目大纲和技术线路的条件下完成代码的编写。谨记项目基于ROS2和Jetson ORIN NX ubuntu22.04开发板。关于最终的避障逻辑，比如停车、减速或者规划避障路径（可以使用nav2包）的执行你可以参考本文件夹下的turtlebot4文件夹。注意，我要完整的从感知，到融合，到决策，最终执行的完整项目代码。有何问题，随时沟通！

4. 项目比较大，但是基础你已经打好了，也就是三个传感器的数据准备好了，你要仔细理解我刚刚的需求，生成从节点到算法到msg到launch文件的完成流程。